import { AgeRangeDisplayPipe } from './age-range-display.pipe';

describe('AgeRangeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeRangeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
