import { FullNameDisplayPipe } from './full-name-display.pipe';

describe('FullNameDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FullNameDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
